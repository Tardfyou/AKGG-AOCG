## 最新更新：2.12

### 对于文件 query.py：

#### 优化点：
##### 优先级调整：
1. **先查精准匹配**，避免计算开销
2. **找不到再查模糊匹配**
3. **最后才查语义匹配**

##### 优化查询：
- **模糊匹配**：使用 `CONTAINS`，对大小写不敏感
- **语义匹配**：使用 `sentence-transformers`，找最接近的事件

#### 语义匹配：
- **sentence-transformers** 计算事件名称的向量
- **余弦相似度** > 0.7 认为匹配成功
